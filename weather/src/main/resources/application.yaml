server:
  port: 8080

spring:
  profiles:
    default: dev
  application:
    name: weather

# cross-origin for actuator
management:
  endpoints:
    web:
      cors:
        allowed-origins: '*'
        allowed-methods: GET
        allowed-headers: '*'